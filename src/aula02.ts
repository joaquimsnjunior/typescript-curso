let numeros: number[] = [1, 2, 3, 4, 5];
// let numeros: Array<number> = [1, 2, 3, 4, 5];
// let numeros: (number|string)[] = [1, 2, 3, 4, 5, "Pombo da Paz"];

// vai ser inserido no final do array
numeros.push(6);

// vai ser inserido no inicio do array
numeros.unshift(0);

console.log(numeros);
